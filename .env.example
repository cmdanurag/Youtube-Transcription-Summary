# Optional: only needed if YouTube starts blocking your IP (see README).
# Get these from https://dashboard.webshare.io/proxy/settings after purchasing
# a "Residential" proxy package.
WEBSHARE_PROXY_USERNAME=
WEBSHARE_PROXY_PASSWORD=
